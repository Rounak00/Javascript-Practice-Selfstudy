// same operators like other languages
+ - / * %

// Some extra 
 **  // 2**3 = 2*2*2;

// Relational operators        here === means same value and datatype.
 == < > <= >= !=  === !==

 {/* bitwise operators  */}
 && || !